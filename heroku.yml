setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - ls
    - python manage.py collectstatic --noinput
run:
  web: gunicorn core.wsgi